# Xcode / xcodegen
Fleetarr.xcodeproj/
Widget/Info.plist
*.xcworkspace/
xcuserdata/
*.xcuserstate
DerivedData/
build/
.build/

# SwiftPM
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Secrets — never commit real credentials or signing config
*.p12
*.mobileprovision
Local.xcconfig

# Release tooling
tools/devops/.build-number   # monotonic CFBundleVersion counter (per-machine)
dist/                        # local release artifacts
